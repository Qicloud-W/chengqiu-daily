# 2026-04-04 日报

## 今日硬结果
- Folio PR #48 `feat: add named route url generation` 已于 2026-04-04 09:33:44Z merge 到 `main`，merge commit 为 `3b207355fb577fb03ef6cc6bb98900f5757a2d79`。
- Folio `main` 在 PR #48 合并后触发的 `php-ci` 已成功，run `23976233530` 绿。
- Folio 当天继续推进到 Request / Validation 主线：PR #50 `feat: add request input and validation alpha baseline` 已打开，head commit 为 `30c684bfdba1d1f4dcd8503e62b6cd582393b45a`。
- PR #50 对应的代码面已有真实落地：补了 `Request` 的 `input()` / `bodyInput()` / `header()`，新增最小 `Validator`（`required` / `string` / `integer` / `array`），并将 `ValidationException` 接入现有 JSON error boundary（422）。
- PR #50 相关 CI 并非空转：`php-ci` 成功（runs `23976748710`、`23976754661`），但 `governance-exec-mvp` 中的 `pr-merge-gate` 失败，因此该 PR 仍未合并。
- linux_toolbox 巡检账本今天形成了 3 个真实提交并落盘到 workspace：
  - `4c99cad` `chore: update subagent scoreboard from patrol`
  - `6834e3a` `chore: update subagent scoreboard patrol`
  - `b145b2f` `chore: refresh linux_toolbox patrol ledger`
- linux_toolbox 现状补核完成：本地仓库存在于 `/Users/qicloud/Code/linux_toolbox`，当前在 `main`，HEAD 为 `11ccb64c86a49edecb1eca1bbe42e5f958b8bb89`；`origin` 实际指向 `https://cnb.cool/Qicloud_W/linux_toolbox.git`，而 `Qicloud-W/linux_toolbox` 这个 GitHub 仓库目前不存在。

## 项目方向变化
- Folio 的主线已经从 Router 三波收尾，切到 Request / Validation alpha 第一刀。Router 不再是当天最核心推进面，重点转为“让 API-first 场景具备最小可写性”。
- linux_toolbox 的巡检口径从“按 GitHub 仓库默认巡检”收敛为“本地仓库 + cnb.cool 才是当前真实数据源”；GitHub 维度目前不能再当成默认事实来源。

## 治理变化
- Folio 的治理门开始真实卡口：PR #50 不是代码没跑，而是出现了“代码 CI 绿、merge gate 没过”的分叉，说明后续推进不能只盯测试结果，还得继续对齐 gate 要求。
- Issue #49 当前仍为 open，且未见标签；后续与 PR #50 的治理信息需要一起补齐，而不是只盯代码实现。
- 今天这份日报按本地仓库、GitHub、会话记录交叉核对后重写，避免把未核实内容写进日报继续污染记录。

## 问题 / Blocker
- Folio PR #50 当前仍是 open，`mergeStateStatus=UNSTABLE`，且 `pr-merge-gate` 失败；代码已落地，但还没有完成可合并闭环。
- linux_toolbox 在 GitHub 上没有对应仓库，因此 GitHub PR / release / Actions run 这三类数据目前没有真实来源。
- linux_toolbox 本地工作树仍脏，存在未跟踪的 `1.php` 与 `1/`，后续巡检会持续受这类本地漂移噪音影响。

## 下一步
- Folio：优先把 PR #50 的 merge gate 要求查清并补齐，过 gate 后再决定是否合并；合并后继续切 Request / Validation 下一刀。
- Folio：继续把 Request / Validation 当作 0.2.x-alpha 的主推进面，Router 只做必要收尾，不再喧宾夺主。
- linux_toolbox：确认后续是否继续以 cnb.cool / 本地 repo 为唯一巡检真源；如果需要 GitHub 维度，就得先补镜像仓库。
- linux_toolbox：清理或隔离 `1.php` 与 `1/` 这类本地漂移，避免它们继续把巡检结果变成重复噪音。